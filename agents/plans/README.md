# ExecPlans

This directory stores active execution plans used by the Architect/Executor/Reviewer workflow.

- One task should map to one plan file.
- Task rows in `agents/context/tasks.yaml` should point here via `execplan`.
- Plan format and quality bar are defined in `agents/prompts/workflows/execplan.md`.
