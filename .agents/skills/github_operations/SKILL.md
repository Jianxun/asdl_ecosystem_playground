---
name: github-operations
description: Run common GitHub and gh CLI workflows safely.
---
## When to use
Use when creating PRs, triaging issues, or checking CI status.
## Instructions
- Use `gh` for GitHub operations.
- Prefer non-destructive git commands.
If skills still don’t appear in /skills, check:
- skill tool permissions in opencode.json (e.g. "permission.skill.*": "allow")
- no duplicate skill names across global/project paths
- frontmatter validity